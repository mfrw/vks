This contains the main virtual-kubelet implementation.

pod.go contains all the methods. The rest are helpers that are only need here.
If helpers are more useful, put them in their own package in pkg/
